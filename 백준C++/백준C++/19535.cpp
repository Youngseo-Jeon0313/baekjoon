#include <bits/stdc++.h>
using namespace std;

vector<int>v;


int main() {
	std::ios_base::sync_with_stdio(0);
	cin.tie(0); cout.tie(0);
	int N;
	cin >> N;
	v.resize(N);

	for (int i = 1; i <= N: i++) {
		cin >> v[i] >> ' ';
	}


	return 0;
}