R,C=map(int,input().split())
arr = [list(map(int,input().split())) for _ in range(R)]
for i in range(R):
    for j in range(C):
        if i==R-1 and j==C-1: continue
        elif i==R-1: 
            first=psum[i][j]
            for k in range(R-2): secondbox=psum[]

        elif j==C-1: firstbox=psum[i][j]

        else: 
            firstbox=psum[i][j]
            secondbox=
            lastbox=
            secondbox=
            lastbox=
            